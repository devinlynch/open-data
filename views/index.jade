extends layout

block content

    center
          div(class="mainDiv", id="mainDiv") 
     
          
          form(action="/start", method="post")
          select(name= "dataset") datalist 
          
          option(value="recall") Details
          
          form(action="/start", method="post")
          input(type='text', name='email', value=email)
          select(name= "details") detaillist  
         

          p Enter Your Email
          input(type='text', name='email', value=email)
          button(type="sumbit") Subscribe